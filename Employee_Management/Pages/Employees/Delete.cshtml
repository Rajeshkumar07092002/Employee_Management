@page
@model Employee_Management.Pages.Employees.DeleteModel
@{
}
@if (Model.errorMessage.Length > 0)
{
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <strong>@Model.errorMessage</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="" aria-label="close"></button>
            </div>
}

@if (Model.successMessage.Length > 0)
{
                <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        <strong>@Model.successMessage</strong>
                        <button type="button" class="btn-close" data-bs-dismiss="" aria-label="close"></button>
                </div>
}




<!-- Button trigger modal -->
@*<a class='btn btn-primary btn-sm' data-bs-toggle="modal" data-bs-target="#deleteEmployee">Delete Employee</a>*@

<!-- Modal -->
@*<div class="modal fade" id="deleteEmployee">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title">Delete Employee</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
          <h3>Are you sure you want to delete this employee?</h3>
      </div>
      <div class="modal-footer">
          <div class="btn-group">
               <a  class="btn btn-outline-primary" href="/Employees/Delete" role="button">Delete</a>
               <a  class="btn btn-danger" href="/Employees/Index" role="button">Cancel</a>
          </div>
      </div>
    </div>
  </div> 
</div>*@