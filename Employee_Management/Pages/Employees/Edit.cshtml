@page
@model Employee_Management.Pages.Employees.EditModel
@{
}

<br />
<h2>Edit Employee</h2>

@if (Model.errorMessage.Length > 0)
{
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
                <strong>@Model.errorMessage</strong>
                <button type="button" class="btn-close" data-bs-dismiss="" aria-label="close"></button>
        </div>
}

<!-- Button trigger modal -->
@*<a class='btn btn-primary btn-sm' data-bs-toggle="modal" data-bs-target="#addEmployee">Edit Employee</a>
<!-- Modal -->
<div class="modal fade" id="addEmployee">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title">Edit Employee</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">

        <form method="post">
            <input type="hidden" name="Id" value="@Model.employeeInfo.Id"/>
            <input type="hidden" name="EmployeeNo" value="@Model.employeeInfo.EmployeeNo"/>

             <div class="row mb-3">
                <label class="col-sm-3 col-form-label">Name</label>
                <div class="col-sm-6">
                  <input  type="text" class="form-control" name="Name" value="@Model.employeeInfo.Name"/>
                </div>
            </div>
             <div class="row mb-3">
                <label class="col-sm-3 col-form-label">Email</label>
                <div class="col-sm-6">
                 <input  type="text" class="form-control" name="Email" value="@Model.employeeInfo.Email"/>
                </div>
            </div>
             <div class="row mb-3">
                <label class="col-sm-3 col-form-label">Username</label>
                <div class="col-sm-6">
                 <input  type="text" class="form-control" name="Username" value="@Model.employeeInfo.Username"/>
                </div>
            </div>
             <div  class="row mb-3">
                <label class="col-sm-3 col-form-label">PasswordStrength</label>
                <div class="col-sm-6">
                    <select  class="form-control" name="passwordStrength" >
                        <option value="0" >options</option>
                        <option value="1" >Low</option>
                        <option value="2" >Medium</option>
                        <option value="3" > High</option>
                    </select>
                </div>  
            </div>
             <div class="row mb-3">
                <label class="col-sm-3 col-form-label">Password</label>
                <div class="col-sm-6">
                 <input  type="text" class="form-control" name="Password" value="@Model.employeeInfo.Password"/>
                </div>
            </div>

            @if (Model.successMessage.Length > 0)
            {
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                            <strong>@Model.successMessage</strong>
                            <button type="button" class="btn-close" data-bs-dismiss="" aria-label="close"></button>
                    </div>
            }

            <div class="row mb-3">
                <div class="offset-sm-3 col-sm-3 d-grid">
                    <button type="submit" class="btn btn-primary">submit</button>
                </div>
                <div class="col-sm-3 d-grid">
                    <a  class="btn btn-outline-primary" href="/Employees/Index" role="button">Cancel</a>
                </div>
            </div>
        </form>

      </div>
    </div>
  </div>
</div>

*@



<form method="post">
    <input type="hidden" name="Id" value="@Model.employeeInfo.Id"/>
    <input type="hidden" name="EmployeeNo" value="@Model.employeeInfo.EmployeeNo"/>

     <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Name</label>
        <div class="col-sm-6">
         <input  type="text" class="form-control" name="Name" value="@Model.employeeInfo.Name"/>
        </div>
    </div>
     <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Email</label>
        <div class="col-sm-6">
         <input  type="text" class="form-control" name="Email" value="@Model.employeeInfo.Email"/>
        </div>
    </div>
     <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Username</label>
        <div class="col-sm-6">
         <input  type="text" class="form-control" name="Username" value="@Model.employeeInfo.Username"/>
        </div>
    </div>
     <div  class="row mb-3">
        <label class="col-sm-3 col-form-label">PasswordStrength</label>
        <div class="col-sm-6">
            <select  class="form-control" name="passwordStrength" >
                <option value="0" >options</option>
                <option value="1" >Low</option>
                <option value="2" >Medium</option>
                <option value="3" > High</option>
            </select>
        </div>  
    </div>
     <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Password</label>
        <div class="col-sm-6">
         <input  type="text" class="form-control" name="Password" value="@Model.employeeInfo.Password"/>
        </div>
    </div>

    @if (Model.successMessage.Length > 0)
    {
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                    <strong>@Model.successMessage</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="" aria-label="close"></button>
            </div>
    }

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a  class="btn btn-outline-primary" href="/Employees/Index" role="button">Cancel</a>
        </div>
    </div>
</form>